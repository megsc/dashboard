<%= form_for(coursecontent) do |f| %>
  <% if coursecontent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coursecontent.errors.count, "error") %> prohibited this coursecontent from being saved:</h2>

      <ul>
      <% coursecontent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :content_name %>
    <%= f.text_field :content_name %>
  </div>

  <div class="field">
    <%= f.label :content_description %>
    <%= f.text_field :content_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
